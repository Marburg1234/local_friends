<!--フォローボタンを部分テンプレート化-->
<!--ゲストユーザーかどうか ゲストである場合、フォローボタンはなし-->
<% if current_user.email == "guest@example.com" %>
<% else %>
<!--フォローボタンの表示-->
  <% if current_user != user %>
    <% if user.followed_by?(current_user) %>
      <%= link_to "フォロー外す", user_relationships_path(user.id), method: :delete, remote:true, data: {"turbolinks" => false}, class: 'btn btn-danger' %>
    <% else %>
      <%= link_to "フォローする", user_relationships_path(user.id), method: :post, remote:true, data: {"turbolinks" => false}, class: 'btn btn-info' %>
    <% end %>
  <% end %>
<% end %>